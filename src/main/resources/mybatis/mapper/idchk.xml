<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chk">
    <select id="search" parameterType="mybatis.vo.MemVO" resultType="mybatis.vo.MemVO">
        SELECT m_id from member_t where m_id = #{m_id}
    </select>
</mapper>
